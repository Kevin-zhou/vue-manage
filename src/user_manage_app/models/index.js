class User {
    // constructor(opts = {}) {
    //     this.account = opts.account || '';
    //     this.avatar = opts.avatar || '';
    //     this.grade = opts.grade || 0;
    //     this.email = opts.email || '';
    //     this.id = opts.id || null;
    //     this.nickname = opts.nickname || '';
    //     this.qq = opts.qq || '';
    //     this.registerTime = __utils.time(opts.registerTime) || null;
    //     this.lastUpdateTime = __utils.time(opts.lastUpdateTime) || null;
    //     this.lastLoginTime = __utils.time(opts.lastLoginTime) || null;
    //     this._settings = opts.settings || 0;
    //     this.sex = opts.sex || 0;
    //     this.type = opts.type || 0;
    //     this.status = opts.status || 0;
    // }
}


export default class Pagination {
    // constructor(opts = {}) {
    //     this.dataList = this.getDataList(opts.dataList);
    //     this.firstPage = opts.firstPage || 0;
    //     this.hasNextPage = opts.hasNextPage || false;
    //     this.hasPreviousPage = opts.hasPreviousPage || false;
    //     this.lastPage = opts.lastPage || 1;
    //     this.navigatePages = opts.navigatePages || 8;
    //     this.pageNum = opts.pageNum || 1;
    //     this.total = opts.total || 0;
    // }

    // getDataList(list) {
    //     const newList = [];
    //     list.map((res, index) => {
    //         newList.push(new User(res));
    //     });
    //     return newList;
    // }
}
